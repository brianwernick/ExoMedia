buildscript {
    repositories {
        mavenLocal()
        jcenter();
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:1.1.2'
        classpath 'com.novoda:bintray-release:0.2.7'
    }
}

allprojects {
    repositories {
        mavenLocal()
        jcenter();
    }
}